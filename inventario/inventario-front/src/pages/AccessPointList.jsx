import RoutersSwitchesList from './RoutersSwitchesList';

/** Listado de puntos de acceso integrado con routers y switches. */
export default function AccessPointList() {
  return <RoutersSwitchesList defaultTab="access-point" />;
}
