import RoutersSwitchesList from './RoutersSwitchesList';

/** Listado de switches con solapas de routers y puntos de acceso. */
export default function SwitchList() {
  return <RoutersSwitchesList defaultTab="switch" />;
}
